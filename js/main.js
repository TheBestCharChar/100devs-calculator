// ? Click number buttons
// ? Numbers are stored and shown on display
// ? store operators
// ? Number Operands are also stored and shown on display
// ? calculations are made
// ? return a result
// ? result is displayed on screen
// ? clear button should clear display and storage
// ? prevent invalid inputs
// https://theodorusandi.hashnode.dev/build-a-calculator-using-4-pillars-of-object-oriented-programming-in-javascript#heading-javascript


function Calculator() {
    this.a = 0
    this.b = 0
}